# Configure this with your PostgreSQL connection string before running Prisma migrations.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
ADMIN_API_TOKEN="change-me"
JWT_SECRET="change-me"
JWT_EXPIRES_IN="7d"
DEFAULT_TENANT_SLUG="onset-media"
PORT=4000
